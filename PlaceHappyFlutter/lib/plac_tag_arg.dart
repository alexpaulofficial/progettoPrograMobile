
//classe che serve per passare gli argomenti da una schermata all'altra con il Navigator
class PlaceTagArg {
  List places;
  List tags;
  var currentUser;

  PlaceTagArg (this.places, this.tags, this.currentUser);

}